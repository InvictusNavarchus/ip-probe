# Server Configuration
# The host/interface to bind the server to
# Use 'localhost' for local development, '0.0.0.0' to bind to all interfaces
HOST=localhost

# The port number for the server to listen on
PORT=3001

# Environment mode (development, production, test)
NODE_ENV=development

# CORS Configuration
# Allowed origins for CORS (comma-separated for multiple origins)
# In production, replace with your actual domain(s)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting
# Maximum number of requests per window (15 minutes)
RATE_LIMIT_MAX=1000

# Logging Configuration
# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Security
# Set to true in production for enhanced security headers
SECURE_HEADERS=false

# Database/Cache Configuration (if needed in the future)
# REDIS_URL=redis://localhost:6379
# DATABASE_URL=postgresql://user:password@localhost:5432/ip_probe

# External API Keys (if needed for enhanced geolocation services)
# MAXMIND_LICENSE_KEY=your_maxmind_license_key
# IPINFO_TOKEN=your_ipinfo_token
